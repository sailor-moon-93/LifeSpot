// Получаем все элементы на странице
var allElements = document.getElementsByTagName("*");

// Считаем количество элементов
var elementCount = allElements.length;

// Выводим alert с количеством элементов
alert("Total elements on the page: " + elementCount);

<input type="text" oninput="    // Получим все элементы input с нашей страницы    let elements = document.getElementsByTagName('input');
 // Получим первый элемент DOM в коллекции
 let element = elements[0]; // Получим строковое значение
 let elementText = element.value;
  // Выведем во всплывающее окно alert(elementText)" size=50 %>